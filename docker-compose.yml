services:
  app:
    build: .
    ports:
      - "3000:3000"
    environment:
      - DATABASE_URL=file:./dev.db
      - JWT_SECRET=your_secret_here
      - NODE_ENV=production
    volumes:
      - ./prisma/dev.db:/app/prisma/dev.db
